bin
/dist/
# a Worker's build output and wrangler's local state, next to its wrangler.toml
build/
.wrangler/
# the copy `dev deploy` runs wrangler on, so its id write-back lands there
wrangler.deploy.toml
# this clone's account facts (dev url) and DEPLOY_SUFFIX
/mise.local.toml
# mise packslip skill links: each developer's `mise skills sync` creates these
# from the active tool versions. .claude/skills/ holds only the vendored copies
# from session.toml.
.claude/skills/.mise-skills.json
.claude/skills/dev
.claude/skills/hk-configure
.claude/skills/hk-debug
