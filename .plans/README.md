# Claude plans

Use a date-time stamp to prefix them all.

Finished plans move to `done/`.

The tool's own origin plan is `grok-oauth-proxy/.plan/dev-tool.md` ("one dev
tool for every repo on this stack", DONE 2026-09-17), written before `dev` was
a repo of its own. From here, `dev`'s plans live here.
